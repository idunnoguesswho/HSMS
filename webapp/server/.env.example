# Copy to server/.env — this file is gitignored, .env is not.

# Set by `npm run set-password -- "your passphrase"`. Do not hand-edit.
HSMS_AUTH_HASH=

# Salus credentials are no longer set here — each client is configured
# individually from the admin page (Clients panel), with its own
# client_id/secret and its own SQLite database under server/data/db/.
